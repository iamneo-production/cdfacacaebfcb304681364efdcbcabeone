<!doctype html>
<html lang = "en- US">
<title> TIC TAC TOE GAME<,
</head>
<body>
<div class = "container">
<table>
<tr>
    <td id = "1"></td>
    <td id = "2"></td>
    <td id = "3"></td>
</tr>
<tr>
    <td id = "4"></td>
    <td id = "5"></td>
    <td id = "6"></td>
</tr>
<tr>
    <td id = "7"></td>
    <td id = "8"></td>
    <td id = "9"></td>
</tr>
</table>
</div>
table {
    border-collaps: collapse : margin: 0 auto;
    margin: o auto;
}
td{
    width: 100px;
    height: 100px;
    text-align: center;
    vertical-align: middle;
    border: 1 px solid black;
    <link rel = "stylesheet" type = "text/css" href = "style.css">
}
</tr>
</body>
</html>
<body>
    <scsript src = "script.js"></script>
        </body>
        let playerSymbol = "x";
        leg gameEnded = false
        for(let i = 1; i<=9; i++) {
            document.getElementById(i.toString()).1ddEventListener(
            "click",
            function(){

            }
            );
        }
if(this.innerHTML === "" && !gameEnded){
    this.innerHTML = playerSymbol;
    this.classList.add(playerSymbol.toLowerCase());
    .x{
        color : blue;
        font-size : 80px;

    }
    .o{
        color :red;
        font-soze: 80px;
        }
    if(playerSymbol === "x")
    playerSymbol = "O"
    else
    playerSymbol = "x"
    function checkWin(){

    }
    for(let i = 0; i< winPos.length;)
}
if(
    document.getElementById(sinpos[i][0]).innteHTML ===playerSymbol&&
    document.getElementById(sinpos[i][1]).innteHTML ===playerSymbol&&
    document.getElementById(sinpos[i][2]).innteHTML ===playerSymbol&&
)
.win{
    background-color : yellow;
    for(let i = 1; i<=9; i++){
        document.getElementById(i.toString().addEventListener(
            "click",
            function(){
                if(this.innerHTML === "" && !gameEnded){
                    this.innerHTML = playerSymbol;
                    this.classList.add(playerSymbol.toLowerCase());
                    checkwin();
                    if(playersymbol === "x")
                    playerSymbol = "O"
                    else
                    playerSymbol = "x"
                }
            }
            <button id + "reset >Reset</button>
            .container {
                display: flex;
                flex-direction: column;

            }
            #reset{
                margin : 48px 40 %
                padding: 20px;
            }
            document.getElementById("reset").addEventListener(
                "click",
                function(){

                }
            );
            for(let i = 1; i<=9; i+=){
               document.getElemtnById(i.toString()).innerHTML = "";
               document.getElemtnById(i.toString()).classList.remove("x");
               document.getElemtnById(i.toString()).classList.remove("o");
               document.getElemtnById(i.toString()).classList.remove("win");
               gameEnded = false;

            }
        )
    }
}
}